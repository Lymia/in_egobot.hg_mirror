>+>-(>)*6
(
  [
    (
      ([-{ ([+{ ([-{ (-)*100(-.)*32 >
      ([-{ ([+{ ([-{ (-)*100(-.)*32 >
      ([-{ ([+{ ([-{ (-)*100(-.)*32 >
      ([-{ ([+{ ([-{ (-)*100(-.)*32 >
        ([-]>)*21
      }])%8 }])%4 }])%2
      }])%8 }])%4 }])%2
      }])%8 }])%4 }])%2
      }])%8 }])%4 }])%2 ->
    )*21
  ]->
)*3
(
  [
    (<)*8
    +<-<+<-
    (-)*26>(+)*26>(-)*26>(+)*26
    (<)*3(-)*58>(+)*58
    (>)*10
    (
      ([-{ ([+{ ([-{ (-)*100(-.)*32 >
      ([-{ ([+{ ([-{ (-)*100(-.)*32 >
      ([-{ ([+{ ([-{ (-)*100(-.)*32 >
      ([-{ ([+{ ([-{ (-)*100(-.)*32 >
        ([-]>)*21
      }])%8 }])%4 }])%2
      }])%8 }])%4 }])%2
      }])%8 }])%4 }])%2
      }])%8 }])%4 }])%2 +>
    )*21
  ]+>
)*18

