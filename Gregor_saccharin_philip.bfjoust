(>+>-)*3>+>
(
  [
    (
        ([-{ ([+{ ([-{ (-)*100(-.)*32 >
        ([-{ ([+{ ([-{ (-)*100(-.)*32 >
        ([-{ ([+{ ([-{ (-)*100(-.)*32 >
        ([-{ ([+{ ([-{ (-)*100(-.)*32 >
          ([-]>)*21
        }])%16 }])%8 }])%4+>
        }])%16 }])%8 }])%4+>
        }])%16 }])%8 }])%4+>
        }])%16 }])%8 }])%4+>
    )*21
  ]->
)*3
(
  [
    (<)*8
    +<-<+<-
    (->+>->++<-<+<-)*13
    (->++<-)*29
    (>)*11
    (
        ([-{ ([+{ ([-{ (-)*100(-.)*32 >
        ([-{ ([+{ ([-{ (-)*100(-.)*32 >
        ([-{ ([+{ ([-{ (-)*100(-.)*32 >
        ([-{ ([+{ ([-{ (-)*100(-.)*32 >
          ([-]>)*21
        }])%16 }])%8 }])%4+>
        }])%16 }])%8 }])%4+>
        }])%16 }])%8 }])%4+>
        }])%16 }])%8 }])%4+>
    )*21
  ]+>
)*18

